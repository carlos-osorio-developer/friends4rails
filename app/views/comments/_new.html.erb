<%= form_with url: "posts/#{post.id}/comments" do |f| %>  
  <%= fields_for 'comment' do %>
    <%= f.label :body, "Comment" %>
    <%= f.text_area :body, :class => "comment_body" %>
    <%= f.submit %>
  <% end %>
<% end %>